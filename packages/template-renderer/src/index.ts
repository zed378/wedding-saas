// @wi/template-renderer
// The generic renderer and the section component library. Imported by BOTH web-app (editor preview) and public-invite (public page) — the shared import is what docs/FRONTEND/04 means by 'the same renderer'.
//
// Populated by a later task; this file exists so the workspace graph is real
// from the start and imports resolve (P0-02).
export const PACKAGE_NAME = "@wi/template-renderer" as const;
