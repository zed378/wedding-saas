// @wi/ui
// Design system: tokens and components (docs/UI-UX/06-09). Also hosts React Email templates (ADR-013).
//
// Populated by a later task; this file exists so the workspace graph is real
// from the start and imports resolve (P0-02).
export const PACKAGE_NAME = "@wi/ui" as const;
