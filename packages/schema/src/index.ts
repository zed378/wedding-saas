// @wi/schema
// Zod schemas, the canonical field-path registry, and the dot-notation resolver. Imported by the API for publish validation and by the frontends for the editor. One definition, so the two cannot drift (ADR-004, ADR-008).
//
// Populated by a later task; this file exists so the workspace graph is real
// from the start and imports resolve (P0-02).
export const PACKAGE_NAME = "@wi/schema" as const;
