# Strict by default: a package may only import what it declares (ADR-005).
# This is the property that stops "works locally, fails in the container".
strict-peer-dependencies=false
auto-install-peers=true
