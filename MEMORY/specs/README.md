This directory holds feature specs for tasks marked `Spec required` in TASKS/.

One file per task: `<task-id>-<slug>.md`, written from `../templates/FEATURE-SPEC-TEMPLATE.md` **before** implementation begins.

Empty until the first `Spec required` task starts.
